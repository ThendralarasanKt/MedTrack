# MedTrack iOS app

The iOS client will be developed here as a SwiftUI/Xcode application.

It shares versioned request, assistant-turn, clarification-card, proposal,
command and receipt contracts from `contracts/` with the Android client and
backend. Patient records, notifications and platform UI remain native to iOS.

The iOS application is not implemented yet.
